name: 'stock_intelligence'
version: '1.0.0'
config-version: 2

profile: 'stock_intelligence'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  stock_intelligence:
    staging:
      +materialized: view
    marts:
      +materialized: table
